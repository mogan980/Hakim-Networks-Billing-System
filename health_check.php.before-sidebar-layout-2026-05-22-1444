<?php
date_default_timezone_set("Africa/Nairobi");

$data = @json_decode(@file_get_contents("http://localhost/mhakim-billing-system/router_status_stable_api.php"), true);

$online = $data["online"] ?? false;
$status = $online ? "ONLINE" : "OFFLINE";
$msg = $data["message"] ?? "No response";
$host = $data["host"] ?? "192.168.88.1";
$identity = $data["identity"] ?? "MikroTik";
$lastSuccess = !empty($data["last_success"]) ? date("Y-m-d H:i:s", $data["last_success"]) : "-";
$lastCheck = $data["time"] ?? date("Y-m-d H:i:s");
$failCount = $data["fail_count"] ?? 0;
?>
<!DOCTYPE html>
<html>
<head>
<title>Router Health Check</title>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
body{margin:0;background:#07131f;color:#e5e7eb;font-family:Arial,sans-serif}
.wrap{max-width:1050px;margin:45px auto;padding:0 18px}
.panel{background:#0f1b2d;border:1px solid #1e293b;border-radius:28px;padding:30px;box-shadow:0 25px 60px rgba(0,0,0,.25)}
.header{display:flex;justify-content:space-between;align-items:center;gap:20px}
h1{margin:0;font-size:34px}
.sub{color:#94a3b8;margin-top:6px}
.status{padding:12px 18px;border-radius:999px;font-weight:900;background:<?=$online?'#064e3b':'#450a0a'?>;color:<?=$online?'#86efac':'#fecaca'?>}
.big{font-size:52px;font-weight:950;color:<?=$online?'#22c55e':'#ef4444'?>;margin:28px 0}
.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin:22px 0}
.card{background:#020617;border:1px solid #1e293b;border-radius:18px;padding:18px}
.card small{color:#94a3b8;font-weight:800}
.card b{display:block;margin-top:10px;font-size:20px}
.actions{display:flex;gap:12px;flex-wrap:wrap;margin-top:24px}
.btn{padding:12px 16px;border-radius:13px;text-decoration:none;font-weight:900;color:white}
.green{background:#16a34a}.blue{background:#2563eb}.gray{background:#334155}
.note{margin-top:20px;background:#052e16;border:1px solid #166534;color:#bbf7d0;border-radius:16px;padding:14px}
@media(max-width:900px){.grid{grid-template-columns:1fr 1fr}.header{display:block}}
</style>
</head>
<body>
<div class="wrap">
  <div class="panel">
    <div class="header">
      <div>
        <h1>💙 Router Health Check</h1>
        <div class="sub">Live MikroTik API connectivity and router status monitor.</div>
      </div>
      <div class="status">● <?=$status?></div>
    </div>

    <div class="big"><?=$status?></div>

    <div class="grid">
      <div class="card"><small>Router Identity</small><b><?=$identity?></b></div>
      <div class="card"><small>Router Host</small><b><?=$host?></b></div>
      <div class="card"><small>Fail Count</small><b><?=$failCount?></b></div>
      <div class="card"><small>Last Check</small><b><?=$lastCheck?></b></div>
    </div>

    <div class="grid">
      <div class="card"><small>Connection Message</small><b><?=$msg?></b></div>
      <div class="card"><small>Last Successful Connection</small><b><?=$lastSuccess?></b></div>
      <div class="card"><small>API Port</small><b>8728</b></div>
      <div class="card"><small>System Timezone</small><b>Africa/Nairobi</b></div>
    </div>

    <div class="note">
      <?=$online
        ? "✅ MikroTik API is reachable. Dashboard, queues, expiry engine and voucher sync can operate normally."
        : "⚠️ MikroTik API is not reachable. Check router API service, password, firewall, and LAN connection."?>
    </div>

    <div class="actions">
      <a class="btn green" href="health_check.php">↻ Refresh Check</a>
      <a class="btn blue" href="queue_control_pro.php">⚡ Queue Control</a>
      <a class="btn blue" href="client_expiry_manager.php">⏱ Client Expiry</a>
      <a class="btn gray" href="noc_final_clean.php">← Back to Dashboard</a>
    </div>
  </div>
</div>
</body>
</html>
