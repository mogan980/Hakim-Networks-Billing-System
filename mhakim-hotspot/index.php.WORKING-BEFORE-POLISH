<?php
require_once "/var/www/html/mhakim-billing-system/config/database.php";

date_default_timezone_set("Africa/Nairobi");

try {
    $stmt = $pdo->query("
        SELECT *
        FROM packages
        WHERE status='active'
        ORDER BY price ASC
    ");
    $packages = $stmt->fetchAll(PDO::FETCH_ASSOC);
} catch (Exception $e) {
    die("Failed to load packages: " . $e->getMessage());
}
?>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Hakim Networks - Buy Internet</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #020617, #052e2b, #064e3b);
            color: #ffffff;
        }

        .container {
            width: 94%;
            max-width: 1150px;
            margin: auto;
            padding: 25px 0;
        }

        .hero {
            text-align: center;
            padding: 25px 15px;
        }

        .hero h1 {
            margin: 0;
            font-size: 34px;
        }

        .hero p {
            color: #d1fae5;
            margin-top: 10px;
        }

        .notice {
            background: rgba(34,197,94,0.15);
            border: 1px solid rgba(34,197,94,0.35);
            padding: 14px;
            border-radius: 14px;
            margin: 15px 0 25px;
            text-align: center;
            color: #dcfce7;
        }

        .packages {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 18px;
        }

        .package {
            background: #ffffff;
            color: #0f172a;
            padding: 20px;
            border-radius: 18px;
            box-shadow: 0 18px 35px rgba(0,0,0,0.25);
        }

        .package h2 {
            margin: 0 0 10px;
            color: #064e3b;
        }

        .price {
            font-size: 28px;
            font-weight: 800;
            color: #16a34a;
            margin-bottom: 8px;
        }

        .speed {
            background: #ecfdf5;
            padding: 10px;
            border-radius: 10px;
            font-weight: bold;
            margin-bottom: 15px;
        }

        input {
            width: 100%;
            padding: 13px;
            border: 1px solid #cbd5e1;
            border-radius: 12px;
            margin-bottom: 10px;
            font-size: 15px;
        }

        button {
            width: 100%;
            padding: 13px;
            border: none;
            border-radius: 12px;
            background: #16a34a;
            color: white;
            font-size: 15px;
            font-weight: 800;
            cursor: pointer;
        }

        button:hover {
            background: #15803d;
        }

        .voucher-box {
            margin-top: 25px;
            background: rgba(255,255,255,0.1);
            padding: 22px;
            border-radius: 18px;
            border: 1px solid rgba(255,255,255,0.15);
        }

        .voucher-box h2 {
            margin-top: 0;
        }

        .support {
            margin-top: 25px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 12px;
        }

        .support a {
            color: #ffffff;
            text-decoration: none;
            background: rgba(255,255,255,0.12);
            padding: 12px;
            border-radius: 12px;
            text-align: center;
        }

        .footer {
            text-align: center;
            margin-top: 25px;
            color: #d1fae5;
            font-size: 14px;
        }

        @media(max-width: 600px) {
            .hero h1 {
                font-size: 26px;
            }

            .package {
                padding: 16px;
            }
        }
    </style>
</head>

<body>

<div class="container">

    <div class="hero">
        <h1>Hakim Networks</h1>
        <p>Fast, affordable internet packages. Pay with M-Pesa and connect instantly.</p>
    </div>

    <div class="notice">
        Select a package, enter your M-Pesa number, pay, then internet activates automatically.
    </div>

    <div class="packages">
        <?php if (empty($packages)): ?>
            <div class="package">
                <h2>No Packages Available</h2>
                <p>Please contact support.</p>
            </div>
        <?php else: ?>
            <?php foreach ($packages as $package): ?>
                <div class="package">
                    <h2><?php echo htmlspecialchars($package["name"]); ?></h2>

                    <div class="price">
                        Ksh <?php echo number_format((float)$package["price"]); ?>
                    </div>

                    <div class="speed">
                        <?php echo htmlspecialchars($package["speed_down"]); ?> Down /
                        <?php echo htmlspecialchars($package["speed_up"]); ?> Up
                    </div>

                    <form method="POST" action="stk_push.php">
                    <input type="hidden" name="client_ip" value="$(ip)">
                        <input type="hidden" name="package_id" value="<?php echo htmlspecialchars($package["id"]); ?>">
                        <input type="text" name="phone" placeholder="Enter M-Pesa number e.g. 0704467699" required>
                        <button type="submit">Buy Internet</button>
                    </form>
                </div>
            <?php endforeach; ?>
        <?php endif; ?>
    </div>

    <div class="voucher-box">
        <h2>Redeem Voucher</h2>
        <p>Enter your voucher code to activate internet instantly.</p>

        <form method="POST" action="redeem_voucher.php">
        <input type="hidden" name="client_ip" value="$(ip)">
            <input type="text" name="voucher_code" placeholder="Enter voucher code" required>
            <button type="submit">Activate Voucher</button>
        </form>
    </div>

    <div class="support">
        <a href="tel:+254704467699">Call Support</a>
        <a href="sms:+254704467699">SMS Support</a>
        <a href="https://wa.me/254704467699" target="_blank">WhatsApp Support</a>
        <a href="mailto:moganhakim3@gmail.com">Email Support</a>
    </div>

    <div class="footer">
        Powered by M.Hakim Billing System • Support: 0704467699
    </div>

</div>

</body>
</html>
